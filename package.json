{
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "release": "bumpp",
    "lint": "oxlint --fix",
    "format": "oxfmt"
  },
  "devDependencies": {
    "bumpp": "^11.1.0",
    "lint-staged": "^17.0.8",
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0",
    "yorkie": "^2.0.0"
  },
  "lint-staged": {
    "*": [
      "oxfmt --no-error-on-unmatched-pattern"
    ],
    "*.js": [
      "oxlint --fix"
    ]
  },
  "packageManager": "pnpm@10.34.4",
  "gitHooks": {
    "pre-commit": "lint-staged"
  }
}
