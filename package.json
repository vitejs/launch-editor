{
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "release": "bumpp",
    "lint": "oxlint --fix",
    "format": "oxfmt"
  },
  "devDependencies": {
    "bumpp": "^11.0.1",
    "lint-staged": "^17.0.5",
    "oxfmt": "^0.52.0",
    "oxlint": "^1.67.0",
    "yorkie": "^2.0.0"
  },
  "lint-staged": {
    "*": [
      "oxfmt --no-error-on-unmatched-pattern"
    ],
    "*.js": [
      "oxlint --fix"
    ]
  },
  "packageManager": "pnpm@10.4.1",
  "gitHooks": {
    "pre-commit": "lint-staged"
  }
}
