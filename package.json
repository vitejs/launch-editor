{
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "release": "bumpp",
    "lint": "oxlint --fix",
    "format": "oxfmt"
  },
  "devDependencies": {
    "bumpp": "^11.1.0",
    "lint-staged": "^17.4.1",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "yorkie": "^2.0.0"
  },
  "lint-staged": {
    "*": [
      "oxfmt --no-error-on-unmatched-pattern"
    ],
    "*.js": [
      "oxlint --fix"
    ]
  },
  "packageManager": "pnpm@10.34.5",
  "gitHooks": {
    "pre-commit": "lint-staged"
  }
}
